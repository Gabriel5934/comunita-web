# Frontend guidance

- Use strict TypeScript and functional React components.
- Add pages under `src/routes` so TanStack Router can generate the route tree.
- Use React Query for server state, React Hook Form with Zod for forms, and Material UI for UI.
- Run `npm run check` and `npm run build` after changes.
